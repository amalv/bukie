pre-commit:
  parallel: true
  commands:
    biome:
      run: bunx biome check {staged_files}
      glob: "*.{js,ts,jsx,tsx,json}"
    vitest:
      run: bun test

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}
